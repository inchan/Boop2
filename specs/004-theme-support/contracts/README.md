# Contracts

This feature does not require API contracts as it is purely a CSS-based theming implementation.

All theme handling is done through:

- CSS Custom Properties (`:root` and `@media (prefers-color-scheme: dark)`)
- CSS Transitions for smooth theme switching
- `@media (prefers-reduced-motion)` for accessibility

No backend API, database schema, or external service contracts needed.
