# Contracts

This feature does not require API contracts as it is purely a client-side UI and CSS implementation.

All opacity handling is done through:

- React state management (`useSettings` hook)
- CSS Custom Properties (`--opacity`)
- localStorage for persistence

No backend API, database schema, or external service contracts needed.
