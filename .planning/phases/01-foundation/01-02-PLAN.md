---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
TabBar에 컨텍스트 메뉴 통합

Purpose: 탭 우클릭 시 ContextMenu 표시 및 상태 관리
Output: TabBar.tsx 수정 (우클릭 이벤트 핸들러 추가)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan:**
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Reference files:**
@src/components/TabBar.tsx
@src/components/ContextMenu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add context menu state to TabBar</name>
  <files>src/components/TabBar.tsx</files>
  <action>
TabBar 컴포넌트에 컨텍스트 메뉴 상태 추가:
- `contextMenu` state: `{ tabId: string, position: { x: number, y: number } } | null`
- `handleContextMenu` 함수: 탭 우클릭 시 호출, e.preventDefault(), position 설정
- `handleCloseContextMenu` 함수: 메뉴 닫기

tab-item div에 onContextMenu 이벤트 핸들러 추가
  </action>
  <verify>TypeScript 컴파일 성공</verify>
  <done>컨텍스트 메뉴 상태 및 핸들러 추가</done>
</task>

<task type="auto">
  <name>Task 2: Render ContextMenu with placeholder items</name>
  <files>src/components/TabBar.tsx</files>
  <action>
ContextMenu 컴포넌트 렌더링:
- import ContextMenu from './ContextMenu'
- contextMenu 상태가 있을 때 ContextMenu 렌더링
- placeholder 메뉴 아이템 (Phase 2에서 실제 액션 연결):
  - "탭 닫기" (disabled: false)
  - "탭 복제" (disabled: false)
  - divider
  - "다른 탭 모두 닫기" (disabled: false)
  - "오른쪽 탭 닫기" (disabled: false)
  - "왼쪽 탭 닫기" (disabled: false)
  - divider
  - "그룹으로 이동" (submenu: 빈 배열 placeholder)

각 onClick에는 console.log로 placeholder 구현 (Phase 2에서 실제 액션 연결)
  </action>
  <verify>`npm run dev`로 앱 실행 후 탭 우클릭 시 메뉴 표시 확인</verify>
  <done>컨텍스트 메뉴 렌더링, placeholder 메뉴 아이템 표시</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>탭 우클릭 시 컨텍스트 메뉴 표시</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. 탭에서 마우스 오른쪽 버튼 클릭
    3. 컨텍스트 메뉴 표시 확인
    4. 메뉴 외부 클릭 또는 Escape로 닫힘 확인
    5. 메뉴 항목들이 올바르게 표시되는지 확인
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` 성공
- [ ] `npm run lint` 성공
- [ ] 탭 우클릭 시 컨텍스트 메뉴 표시
- [ ] 외부 클릭/Escape로 메뉴 닫힘
</verification>

<success_criteria>
- TabBar에 컨텍스트 메뉴 통합 완료
- 우클릭 이벤트 핸들러 동작
- Phase 1 Foundation 완료
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
