---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
ContextMenu 컴포넌트 생성 및 스타일링

Purpose: 탭 우클릭 시 표시될 컨텍스트 메뉴의 기본 UI 컴포넌트 구축
Output: ContextMenu.tsx + ContextMenu.css
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Codebase context:**
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

**Reference files:**
@src/components/TabBar.tsx
@src/components/TabBar.css

**Established patterns:**
- React 19 function components
- Co-located CSS files
- BEM-inspired class names
- CSS custom properties for theming
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContextMenu component</name>
  <files>src/components/ContextMenu.tsx</files>
  <action>
Create a reusable ContextMenu component with:
- Props: `items` (array of menu items), `position` ({x, y}), `onClose` callback
- MenuItem type: `{ label: string, onClick: () => void, disabled?: boolean, divider?: boolean, submenu?: MenuItem[] }`
- 위치 계산: viewport 경계를 벗어나지 않도록 조정
- 외부 클릭 시 닫기 (useEffect + document.addEventListener)
- Escape 키로 닫기
- createPortal로 document.body에 렌더링 (z-index 문제 방지)

Avoid:
- 외부 라이브러리 사용 금지 (순수 React)
- inline styles 대신 CSS classes 사용
  </action>
  <verify>TypeScript 컴파일 성공: `npx tsc --noEmit`</verify>
  <done>ContextMenu.tsx 생성, Props 타입 정의, 외부 클릭/Escape 핸들링 구현</done>
</task>

<task type="auto">
  <name>Task 2: Create ContextMenu styles</name>
  <files>src/components/ContextMenu.css</files>
  <action>
기존 TabBar.css의 group-dropdown-menu 스타일을 참고하여 작성:
- `.context-menu` - 메뉴 컨테이너 (position: fixed, background, border-radius, box-shadow)
- `.context-menu-item` - 메뉴 항목 (padding, hover 효과)
- `.context-menu-item.disabled` - 비활성화 상태
- `.context-menu-item.has-submenu` - 서브메뉴 표시 화살표
- `.context-menu-divider` - 구분선
- CSS custom properties 사용 (--bg-color, --text-color 등)
- Dark mode 지원 (@media prefers-color-scheme)
  </action>
  <verify>`npm run lint` 성공</verify>
  <done>ContextMenu.css 생성, 기존 테마와 일관된 스타일</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` 성공
- [ ] `npm run lint` 성공
- [ ] ContextMenu.tsx, ContextMenu.css 파일 존재
</verification>

<success_criteria>
- ContextMenu 컴포넌트 생성 완료
- TypeScript 타입 정의 완료
- 외부 클릭/Escape 닫기 구현
- 기존 테마와 일관된 CSS
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
